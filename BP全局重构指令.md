# 仓颉 BP 网页全局重构指令

> 这是一份完整的结构重构方案。所有 section 的改动、新增、删除、合并都在这里。

---

## 一、新叙事框架总览

| 步骤 | 标题 | 核心问题 | 对应内容 |
|------|------|---------|---------|
| Step 1 | **What** — 我们做的是什么 | 一句话价值主张 | Hero 首屏 |
| Step 2 | **Why Now** — 行业出了什么问题 | 全球性劳动力危机 | 「两份 Offer」四层交互体验 |
| Step 3 | **How** — 我们怎么解决 | 遥操作 + 套利 + 数据飞轮 | 合并原 Section 02/03/04 |
| Step 4 | **Why Us** — 为什么是我们 | 中国优势 + 团队 | 合并原 Why China + Team |

**被删除的独立内容：**
- 原 VisionSection 的市场规模柱状图（MarketBarChart）→ 删除，不再用上来就看大数据的方式讲市场
- 原 VisionSection 的物流市场规模表格（LogisticsMarketSizing）→ 移入「两份 Offer」交互体验的第四层（蓝海市场），作为"缺口有多大"的量化支撑
- 原 VisionSection 的三段叙事文案（确定性 vs 不确定性）→ 替换为 v1 指令中的"生产力去中心化"三段文案 + 飞轮图，但位置移入 Step 3（How）

---

## 二、左侧叙事索引条（新增全局组件）

### 功能

页面左侧固定一个纵向导航条，显示当前 section 在整个叙事链中的位置。投资人滚动时自动高亮当前步骤。

### 视觉设计

- 固定定位 `fixed left-0 top-1/2 -translate-y-1/2 z-40`
- 宽度约 48-60px，不遮挡主内容
- 四个节点纵向排列，每个节点是一个小圆点 + 短标签
- 当前 section 对应的节点高亮（荧光绿 + 微 glow），其他节点灰色
- 节点之间用细线连接（`border-l border-white/10`），当前节点以上的线段变亮
- 点击节点可跳转到对应 section（smooth scroll）
- 移动端隐藏（`hidden md:flex`）

### 四个节点

```
① What — 我们做什么
② Why Now — 为什么是现在
③ How — 怎么解决
④ Why Us — 为什么是我们
```

### 技术

- 新组件：`StoryNav.tsx`
- 用 `IntersectionObserver` 监听四个 section 的进出视口，自动更新高亮状态
- 放在 `Home.tsx` 的 `<Navbar />` 下方，和所有 section 平级

---

## 三、Step 1 — What（Hero 首屏）

### 对应组件：`HeroSection.tsx`

### 改动

**保留不动的：** 粒子背景、打字机效果、scroll indicator、整体布局和动画。

**文案微调：**

原副标题：
```
60 分钟开箱即用的半价机器人员工
```

建议改为：
```
当没有人愿意做这些工作时，让机器去做。
```

或者双行：
```
让机器去做人类不再愿意做的工作
当远程操控的人力成本只有现场的 1/3
```

> 逻辑：原副标题是产品特性（开箱即用、半价），新版本先回答"为什么这件事重要"（没人愿意做），再暗示解决方案（远程操控 + 成本优势）。投资人看完 Hero 应该感受到的是"这是一个真实的问题 + 这些人有一个聪明的解法"，而不是"这是一个机器人产品"。

**原来的标语保留为更小的辅助文字：**
```
遥操打底 × 劳动力套利 × Robotics Native 数据垄断
```
这行不变，继续作为 Hero 下方的 tagline。

**"60 分钟开箱即用"不丢弃**，移到 Step 3（How）的遥操作部署部分，作为产品特性的具体描述。

**CTA 按钮文案：**
```
旧：查看商业账本 → Unlock the Economic Engine
新：往下看，这碗饭已经没人端了 ↓
```
或者更克制：
```
了解我们在解决什么问题 ↓
```

> 注意：这是一个方向建议，Edward 你来拍板最终措辞。核心原则是 Hero 首屏回答 What + Why it matters，不要提前进入 How。

---

## 四、Step 2 — Why Now（「两份 Offer」交互体验）

### 对应组件：`VisionSection.tsx`（重写）+ `LaborCrisisPanel.tsx`（新增）

### 这个 section 的唯一任务

让投资人在 60 秒内理解：全球范围内，仓储/物流/搬运类岗位的劳动力供给正在系统性崩塌，这是人口结构 × 代际偏好 × 经济理性共同作用的不可逆趋势。

### 结构

Section 主体只需要一个简短的引导 + 入口卡片，点击后打开四层交互体验（全屏 overlay）。

**Section 主体文案：**

小标题：`Step 02 — Why Now`

大标题：
```
一碗没人愿意端的饭
```

副标题：
```
全球仓储物流行业正在经历一场系统性的劳动力崩塌。
这不是中国的问题，不是日本的问题，而是所有老龄化社会的共同命运。
```

然后直接放入口卡片，引导进入四层交互体验。

**四层交互体验的内容**：见《劳动力危机交互层-四层结构-v2.md》，不在此重复。

### 关键变化

- **"为什么在中国"三张卡片不再放在这里**→ 移到 Step 4（Why Us）
- **原市场规模柱状图删除**
- **原物流市场规模表格**→ 移入交互体验的第四层
- **原"生产力去中心化"三段叙事**→ 移到 Step 3 开头作为 How 的总纲

---

## 五、Step 3 — How（合并原 Section 02/03/04）

### 对应组件：需要重新组织，建议拆为 `SolutionSection.tsx`（新建，合并内容）

### 这个 section 的任务

讲清楚仓颉的解决方案和商业模型。投资人看完应该理解：遥操作是起点不是终点，套利是引擎不是天花板，数据飞轮让每一天的运营都在构建通往全自主的壁垒。

### 信息架构（按叙事顺序）

#### 3a. 总纲 — 生产力的去中心化

**小标题：** `Step 03 — How We Solve It`

**大标题：**
```
生产力的去中心化
从工厂到每一个仓库
```

**三段叙事文案**（从 v1 VisionSection 改动指令搬过来）：
- 历史类比（大型机→PC）
- 断裂点（$340亿被锁死在结构化场景）
- 仓颉的解锁方式

**飞轮图**（从 v1 VisionSection 改动指令搬过来）：
- 四节点闭环 + 三层轨道 + metric 卡片

#### 3b. 为什么是遥操作 — 两条路径的分野

**来源：** 原 `IllusionSection.tsx` + 原 `ParadigmSection.tsx` 部分内容

**核心论点：**
- 仿真路线的幻觉：计算机无法真实模仿真实物理世界
- UMI 采集路线的局限：人和机器人的感知器/执行器不同
- 我们的选择：从第一天就让机器人在真实场景里迭代进化

**路径分野图（原 todo.md 中未完成项，需新建）：**
- 左侧：仿真环境搭建 → 仿真数据训练 → Sim-to-Real 迁移 → 真实场景出问题 → 返修 → 仿真调参（无限循环，标红）
- 右侧：第一天进入真实场景 → 遥操干活赚钱 → 高保真数据积累 → 模型持续进化 → 规模化扩张（正循环，标绿）

**对比表格**（从原 IllusionSection 保留）：
- 硬件精度：航天级谐波减速器 vs 80 年前已经成熟的行星减速器
- 容错机制：无反馈盲操 vs 高频人在回路闭环纠错
- 评价标准：动作精度够不够 vs 任务能不能做成
- 数据定义权：算法工程师在实验室定义 vs 真实场景中的物理交互定义

**Sim vs Real / UMI 对比图**（原 IllusionSection 已有的图片素材保留）

#### 3c. 商业引擎 — 15 个月回本

**来源：** 原 `EngineSection.tsx`

**保留的内容：**
- 跨国劳动力套利对比（中国二线 ¥7,000-8,000 vs 越南 ¥2,000-3,000）
- 15 个月回本公式（BOM ¥60,000 ÷ 月净套利 ¥4,000 = 15 个月）
- 行业玩家横向滚动卡片（1X、Reflex、Tesla Optimus、Telexistence）

**位置调整：** 不再是独立 section，而是 Step 3 内的一个子板块。

#### 3d. 数据飞轮的细节（可选，如果飞轮图已经在 3a 讲清楚了可以省略）

如果投资人需要更多细节，可以用一个展开面板补充：
- 遥操作产生什么数据（关节角度、力矩、视觉流、任务标注）
- 这些数据如何喂给 ACT/Diffusion Policy
- 自主率提升的里程碑（30% → 50% → 80%）

---

## 六、Step 4 — Why Us（合并原 Why China + Team）

### 对应组件：建议合并为 `WhyUsSection.tsx`（新建）或保留两个组件但归入同一个视觉 section

### 信息架构

#### 4a. 为什么在中国

**小标题：** `Step 04 — Why Us`

**大标题：**
```
在全球最完整的硬件供应链
与最庞大的非结构化场景中
```

**三张卡片**（从原 VisionSection 搬过来，内容不变）：
1. 大量优质溢出的工程师人才
2. 最全面的非结构化场景
3. 电动汽车及智能制造产业的溢出

**逻辑变化：** 原来放在 Vision Section 里，是"行业洞察"的一部分。现在放在 Why Us 里，回答的是"为什么我们能做成这件事"——中国恰好具备做这件事的所有结构性优势。

#### 4b. 团队

**来源：** 原 `TeamSection.tsx`

**大标题改为：**
```
从 Day One 就靠机器人创业赚到过钱的 Team
```

**副标题：**
```
一起打过比赛、睡过走廊、分过钱的 00 后机器人团队
```

**其余内容保留不变：** 团队成员卡片、RoboMaster 背景、视频/内容预留框架。

---

## 七、Footer

**保留不变。** "让我们创造自由的世界，让机器做回机器，让人做回人" —— 这句话现在和交互体验第四层的收束语形成呼应。

---

## 八、页面组件映射（旧→新）

| 原组件 | 处置 | 新位置 |
|--------|------|--------|
| `Navbar.tsx` | 保留 | 保留 |
| **新增** `StoryNav.tsx` | 新建 | 左侧固定索引条 |
| `HeroSection.tsx` | 微调文案 | Step 1 — What |
| `VisionSection.tsx` | **重写** | Step 2 — Why Now（引导+入口） |
| **新增** `LaborCrisisPanel.tsx` | 新建 | Step 2 的全屏交互体验 |
| `IllusionSection.tsx` | 合并入 Step 3 | Step 3b — 路径分野 |
| `ParadigmSection.tsx` | 合并入 Step 3 | Step 3b — 闭环纠错哲学 |
| `EngineSection.tsx` | 合并入 Step 3 | Step 3c — 商业引擎 |
| **新增** `SolutionSection.tsx` | 新建（整合 3a-3d） | Step 3 — How |
| `TeamSection.tsx` | 移入 Step 4 | Step 4b — 团队 |
| Why China 卡片 | 从 Vision 移出 | Step 4a — 为什么在中国 |
| `FooterSection.tsx` | 保留 | Footer |

### Home.tsx 新结构

```tsx
<div>
  <Navbar />
  <StoryNav />          {/* 新增：左侧叙事索引 */}
  <HeroSection />       {/* Step 1: What */}
  <Divider />
  <VisionSection />     {/* Step 2: Why Now + 交互入口 */}
  <Divider />
  <SolutionSection />   {/* Step 3: How（合并原 02/03/04） */}
  <Divider />
  <WhyUsSection />      {/* Step 4: Why Us（Why China + Team） */}
  <Divider />
  <FooterSection />
</div>
```

---

## 九、原 Navbar 导航项更新

```ts
// 旧
{ id: "hero", label: "首页" },
{ id: "vision", label: "行业洞察" },
{ id: "illusion", label: "数据定义权" },
{ id: "paradigm", label: "第一性原理" },
{ id: "engine", label: "商业引擎" },
{ id: "team", label: "团队" },

// 新
{ id: "hero", label: "首页" },
{ id: "why-now", label: "为什么是现在" },
{ id: "how", label: "解决方案" },
{ id: "why-us", label: "为什么是我们" },
```

---

## 十、关联文档

本文档是全局结构指令。以下两份文档包含具体 section 级别的详细规格：

1. **《VisionSection-改动指令.md》** — Step 3a 的飞轮图和三段叙事文案的详细设计
2. **《劳动力危机交互层-四层结构-v2.md》** — Step 2 的四层交互体验详细设计

三份文档配合使用。
